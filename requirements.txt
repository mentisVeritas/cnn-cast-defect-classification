# Windows: install PyTorch (GPU) first, then this file (see README.md)
#
#   .venv\Scripts\activate.bat
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements.txt
#
# CPU only (no NVIDIA):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

numpy>=1.24.0,<3
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
PyYAML>=6.0
Pillow>=10.0.0
tqdm>=4.65.0

# Optional: Streamlit demo (app/streamlit_app.py)
streamlit>=1.28.0
