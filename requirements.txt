# Windows — dependencies (PyTorch installed separately, see below)
#
# 1) Create venv and activate:
#    py -3.11 -m venv .venv
#    .venv\Scripts\activate
#
# 2) PyTorch (pick ONE line):
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
#
# 3) Rest of the project:
#    pip install -r requirements.txt

numpy>=1.24.0,<3
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.12.0
PyYAML>=6.0
Pillow>=10.0.0
tqdm>=4.65.0

# Optional: Streamlit demo (app/streamlit_app.py)
streamlit>=1.28.0
